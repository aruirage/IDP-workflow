# NeosAI 日本保险 IDP 工作流配置 — 产品需求文档（PRD）

## 第 6 章｜功能详细说明

### 6.01 业务场景设置

三步向导：Step1 案件集约规则，Step2 跳转工作流画布，Step3 输出字段与发布。新建、编辑已发布、复制三类场景共用流程；影响执行逻辑的变更将工作流测试结论置为未测试。

#### 功能点

1. 场景列表：新建、复制、重命名、删除、关键词检索；复制后状态为待确认，须复核失效引用。
2. Step1：配置名称、说明、账票集合（1～20）、主账票与主字段、关联账票字段关系、匹配策略；支持关联检查与 AI 自动关联。
3. Step2：进入工作流画布；画布读取 Step1 账票与关系，限制节点可选范围。
4. Step3：勾选并排序输出字段（OCR 字段、标准字段、工作流结果字段等）。
5. 发布：三步校验通过且工作流测试成功后方可发布；新案件使用发布后配置。

#### 页面与入口


| 场景      | 菜单/入口            | 角色                  |
| ------- | ---------------- | ------------------- |
| 场景列表    | 业务规则配置 / 业务场景设置  | 系统管理员、业务规则配置人员、只读人员 |
| Step1～3 | 场景详情 / 各步骤       | 系统管理员、业务规则配置人员      |
| 工作流画布   | Step2 / ワークフロー設定 | 同上                  |




#### 按钮与启用条件


| 按钮   | 界面文案（日语）    | 适用场景    | 启用条件        | 点击后           |
| ---- | ----------- | ------- | ----------- | ------------- |
| 新建   | ＋ 業務シーン追加   | 场景列表    | 有编辑权限       | 创建草稿并进入 Step1 |
| 复制   | コピー         | 场景列表菜单  | 有编辑权限       | 生成待确认草稿       |
| 重命名  | 名称変更        | 场景列表菜单  | 有编辑权限       | 修改显示名称        |
| 删除   | 削除          | 场景列表菜单  | 有编辑权限       | 删除场景及本地配置     |
| 关联检查 | 関連チェック      | Step1   | 已选主账票且有关联账票 | 校验字段关系与失效引用   |
| 添加账票 | + 帳票追加      | Step1   | 有编辑权限       | 打开账票类型选择      |
| 自动关联 | AI 自動関連付け   | Step1   | 关联账票不少于 2 件 | 自动生成字段关联      |
| 下一步  | 保存して次へ / 次へ | 各步      | 当前步无阻断错误    | 进入下一步         |
| 上一步  | 前へ          | Step2/3 | 始终可用        | 返回上一步         |
| 打开画布 | ワークフロー設定    | Step2   | 有查看权限       | 进入工作流画布       |
| 选择字段 | 出力項目を設定     | Step3   | 存在可选字段      | 侧栏选择输出字段      |
| 发布   | 公開          | 发布确认    | 三步通过且测试可发布  | 标记已发布         |




#### 字段与参数规格


| 字段/参数      | 控件        | 必填  | 数据来源与枚举        | 校验与转换           | 下游消费          |
| ---------- | --------- | --- | -------------- | --------------- | ------------- |
| 业务场景 ID    | 只读        | 是   | 系统生成           | 保存后生成           | 上传、案件集约、工作流实例 |
| 业务场景名称     | 文本        | 是   | 用户输入           | 1～64 字符；租户内唯一   | 上传、场景列表、画布    |
| 业务场景说明     | 多行        | 否   | 用户输入           | 最长 256 字符       | 场景列表          |
| 场景状态       | 只读标签      | 是   | 草稿、待确认、可发布、已发布 | 由保存/测试/发布更新     | 场景列表、上传过滤     |
| 账票类型       | 多选        | 是   | 账票类型设置         | 1～20；失效标记待确认    | 画布、案件集约       |
| 主账票 / 主字段  | 单选        | 是   | 已选账票及 OCR 字段   | 主账票唯一           | 案件集约          |
| 关联账票字段关系   | 下拉组       | 是   | 关联账票 OCR 字段    | 每关联账票至少一组       | 案件集约          |
| 匹配策略       | 下拉        | 是   | 精确匹配、近似匹配      | 默认精确            | 案件集约          |
| 工作流测试结论    | 只读        | 是   | 未测试、测试成功、测试失败  | Step1/2 变更后回未测试 | 发布校验          |
| 输出字段 / 显示名 | 勾选排序 + 文本 | 是   | OCR、标准、工作流结果字段 | 至少一项；显示名场景内唯一   | 导出            |




#### 列表展示

场景卡片展示名称与状态徽章（公開済み / 下書き）；工具栏关键词检索，占位「業務シーン名で検索」。无数据时展示空状态；无权限隐藏新建。

#### 异常与边界


| 场景         | 条件          | 用户提示 / 处理                                 |
| ---------- | ----------- | ----------------------------------------- |
| 名称为空       | 保存或发布未填名称   | 「業務シーン名を入力してください」                         |
| 名称重复       | 同租户同名       | 「同じ業務シーン名が既に存在します」                        |
| 未选账票       | Step1 无关联账票 | 「関連帳票を1件以上追加してください」                       |
| 账票超限       | 超过 20 件     | 「帳票は最大20件までです」                            |
| 无主账票/主字段   | 未指定         | 「主帳票を1件選択してください」/「業務キーを選択してください」          |
| 主账票未关联     | 有账票未挂到主账票   | 「主帳票に未関連の帳票があります：{列表}」                    |
| 关系缺失       | 关联账票无字段关系   | 「この帳票間関連のフィールドを追加してください」                  |
| 工作流未测试/未配置 | 测试失败或画布无节点  | 「ワークフローテストを完了してください」/「ワークフローノードを設定してください」 |
| 输出字段为空     | Step3 未选    | 「エクスポート字段を1件以上選択してください」                   |
| 发布确认       | 点击发布        | 标题「業務シーンを公開しますか？」；按钮「公開」「キャンセル」           |
| 并发保存       | 草稿已被他人更新    | 「設定が更新されています。再読み込みしてからもう一度お試しください」        |




### 6.02 工作流画布与可编排节点

画布只编排案件集约完成后的处理段；不编排上传、案件集约、导出。节点规则明细在账票类型、数据映射、AI 检证等设置页维护，画布仅配置外壳、开关、引用与分支。

#### 功能点

1. 画布编辑：添加/插入/连接/删除节点与连线、自动整列；逻辑变更置测试为未测试。节点标题固定为类型名（如「条件判断」），不拼接上下文名。
2. 节点选择器（浮层）：业务 Agent（AI 前处理、OCR 抽出、数据映射、AI 检证）、控制（条件判断、人工确认、通知）、扩展（自定义函数）、端点（开始、结束）。入口：右下角「ノードを追加」或连接点。
3. 节点编辑面板：按类型展示参数；底部折叠「出力変数」只读展示（变量键 + 类型 + 界面文案，不展示取值说明）。数据映射、AI 检证仅提供跳转设置页，不在面板内编规则。
4. 变量引用：条件、通知、自定义函数入参仅可选当前路径可达上游输出变量；失效阻断发布。
5. 分支：条件判断 IF/ELIF/ELSE；人工确认固定完成、补件、案件终止三出口。
6. 人工确认：确认对象由上游最近业务 Agent 自动判定；须接在前处理/OCR/AI 检证之后（穿透条件节点检测），否则测试失败。
7. 工作流测试：对象=当前草稿 + 内置标准集约済み用例；不跑上传/集约，不写正式案件/待办。弹窗左侧只读用例与节点详情，右侧「Workflow 実行」时间线展示可达路径执行结果。



#### 页面与入口


| 场景     | 菜单/入口          | 角色                  |
| ------ | -------------- | ------------------- |
| 工作流画布  | 业务场景设置 / Step2 | 系统管理员、业务规则配置人员、只读人员 |
| 节点编辑面板 | 画布右侧           | 配置人员                |
| 测试弹窗   | 画布 / テスト       | 配置人员                |




#### 按钮与启用条件


| 按钮           | 界面文案（日语）     | 适用场景      | 启用条件      | 点击后        |
| ------------ | ------------ | --------- | --------- | ---------- |
| 添加节点         | ノードを追加       | 画布工具栏/连接点 | 有编辑权限     | 打开选择器      |
| 插入节点         | ここにノードを挿入    | 连线        | 类型兼容      | 插入并重连      |
| 删除节点/连线      | 削除 / この接続を削除 | 选中项       | 非唯一开始节点   | 删除并置未测试    |
| 自动整理         | ノードを自動整列     | 画布        | 存在节点      | 重布局并重算连线路径 |
| 保存           | 保存           | 画布        | 有编辑权限     | 保存节点与连线    |
| 测试 / 执行测试    | テスト / テスト実行  | 测试弹窗      | 有用例且画布有节点 | 校验后模拟执行    |
| 定位错误         | エラー箇所へ移動     | 测试时间线     | 存在失败节点    | 高亮画布节点     |
| 前へ / 次へ / 公開 | 前へ / 次へ / 公開 | 画布/向导     | 见 6.01    | 导航或发布      |




#### 字段与参数规格


| 字段/参数          | 控件         | 必填      | 数据来源与枚举               | 校验与转换     | 下游消费   |
| -------------- | ---------- | ------- | --------------------- | --------- | ------ |
| AI 前处理能力       | 开关+多选      | 至少一项    | 画像旋转/补正/分割/排序；可指定对象账票 | 全关阻断保存    | AI 前处理 |
| OCR 抽出开关       | 开关列表       | 至少一项    | Step1 关联账票            | 关闭账票仅分类   | OCR 抽出 |
| 数据映射/AI 检证     | 跳转链接       | —       | 各设置页                  | 面板不编规则    | 运行时读取  |
| AI 检证模块        | 开关列表       | 至少一项    | 六项检证模块                | 至少开启一项    | AI 检证  |
| 条件变量/分支        | 级联+表达式     | 条件节点必填  | 可达上游变量                | ELSE 无表达式 | 条件判断   |
| 审查角色           | 下拉         | 人工确认必填  | 案件担当者、角色等             | —         | 我的任务   |
| 通知方式/收件人/标题/正文 | 表单+变量插入    | 通知节点必填  | 系统通知/邮件               | 变量须有效     | 站内信、邮件 |
| 自定义函数          | 入参表+脚本+出参表 | 脚本与出参必填 | 用户定义                  | 出参键不重复    | 下游节点   |


开始节点：系统固定，只读展示三种启动情形（新規起動/続行/再実行）；进入 Step2 自动添加，不可删。结束节点：系统固定，只读展示分岐ステータス等四项运行时输出说明。

#### 列表展示


| 区块      | 展示内容            | 说明                 |
| ------- | --------------- | ------------------ |
| 画布节点    | 类型、一行摘要         | 标题为固定类型名           |
| 连线      | 分支名（条件/人工确认）    | 回流允许，测试时标注環状パス     |
| 出力変数折叠区 | 变量键、类型、界面文案     | 不展示取值说明            |
| 测试弹窗    | 左：用例+节点详情；右：时间线 | 孤立/不可达节点不在时间线，画布高亮 |




#### 案件级节点入口校验

数据映射与 AI 检证为案件级节点；引擎在调度时、执行业务逻辑前校验本案全部逻辑文件到齐。配置端不在节点面板展示校验项。


| 校验项        | 数据映射 | AI 检证 | 说明           |
| ---------- | ---- | ----- | ------------ |
| 集约关联成功     | 是    | 是     | 见总 PRD 文件状态  |
| 文件级确认待办已办结 | 是    | 是     | 前处理/OCR 确认待办 |
| 数据映射已完成    | —    | 路径可达时 | 无映射节点则跳过     |


未通过：节点进度为等待，文件状态变更后自动重试。测试弹窗默认假定用例内文件已到齐。

#### 变量作用域与引用规则


| 作用域   | 界面标注  | 引用路径示例                 |
| ----- | ----- | ---------------------- |
| 案件级   | 案件    | `{节点变量名.case.键}`       |
| 文件级   | ファイル  | `{节点变量名.files[].键}`    |
| 账票类型级 | 帳票タイプ | `{节点变量名.docTypes[].键}` |


规则：仅选可达上游变量；对象/数组须选子字段再比较；上游删除或规则失效标记错误并阻断发布。

#### 各节点输出变量

配置端「出力変数」折叠区：第一行变量键 + 类型；第二行界面文案（日语）。带「配置影响」的项在开关关闭或规则未命中时可能为空或 skipped。

#### 开始节点


| 变量键                    | 界面文案（日语）  | 类型     | 作用域  |
| ---------------------- | --------- | ------ | ---- |
| caseNo                 | 案件番号      | String | 案件   |
| businessScene          | 業務シーン     | String | 案件   |
| triggerType            | トリガー種別    | Enum   | 案件   |
| caseStatus             | 案件状態      | Enum   | 案件   |
| pendingFileCount       | 待処理ファイル件数 | Number | 案件   |
| docTypes[].docTypeList | 帳票タイプ一覧   | Array  | 账票类型 |




#### 结束节点


| 变量键              | 界面文案（日语） | 类型       | 作用域 |
| ---------------- | -------- | -------- | --- |
| caseStatus       | 案件状態     | Enum     | 案件  |
| finalResult      | 最終処理結果   | Enum     | 案件  |
| hasOpenItems     | 未完了事項あり  | Boolean  | 案件  |
| resultFileStatus | 成果ファイル状態 | Enum     | 案件  |
| endedAt          | 終了時刻     | DateTime | 案件  |




#### AI 前处理节点

前処理オプション四项（画像回転/補正/分割/並び替え），可指定对象账票；账票类型设置第 4 步可关闭分割/整列，优先生效。


| 变量键                        | 界面文案（日语） | 类型      | 作用域 | 配置影响          |
| -------------------------- | -------- | ------- | --- | ------------- |
| preprocessStatus           | 前処理ステータス | Enum    | 案件  | 有失败→failed    |
| manualReviewRequired       | 人工確認必要   | Boolean | 案件  | 低置信等          |
| lastFailureReason          | 最終失敗原因   | String  | 案件  | 失败时写入         |
| files[].processedFile      | 処理済みファイル | File    | 文件  | 仅处理过的文件       |
| files[].preprocessStatus   | 前処理状態    | Enum    | 文件  | 未命中能力→skipped |
| files[].preprocessWarnings | 前処理警告    | Array   | 文件  | —             |




#### OCR 抽出节点

帳票別設定按 Step1 账票逐项开关；关闭类型仅分类不 OCR。


| 变量键                  | 界面文案（日语）   | 类型      | 作用域 | 配置影响         |
| -------------------- | ---------- | ------- | --- | ------------ |
| ocrStatus            | OCRステータス   | Enum    | 案件  | —            |
| manualReviewRequired | 人工確認必要     | Boolean | 案件  | —            |
| ocrFields            | OCRフィールド集約 | Object  | 案件  | 仅开启类型        |
| files[].ocrFields    | OCRフィールド   | Object  | 文件  | 关闭类型 skipped |
| files[].ocrStatus    | OCR状態      | Enum    | 文件  | —            |


ocrFields 子键由账票类型 OCR 字段动态生成，画布不列举。

#### 数据映射节点


| 变量键              | 界面文案（日语）   | 类型     | 作用域 | 配置影响        |
| ---------------- | ---------- | ------ | --- | ----------- |
| mappingStatus    | マッピングステータス | Enum   | 案件  | 无规则→skipped |
| standardFields   | 標準フィールド    | Object | 案件  | 键=数据映射标准变量名 |
| mappingConflicts | 競合一覧       | Array  | 案件  | —           |
| mappingErrors    | マッピングエラー   | Array  | 案件  | —           |




#### AI 检证节点

検証モジュール六项开关对应 AI 检证设置规则库。


| 变量键                    | 界面文案（日语）  | 类型      | 作用域 | 配置影响        |
| ---------------------- | --------- | ------- | --- | ----------- |
| verifyStatus           | AI検証ステータス | Enum    | 案件  | 全关→skipped  |
| supplementRequired     | 補件必要      | Boolean | 案件  | —           |
| manualReviewRequired   | 人工確認必要    | Boolean | 案件  | —           |
| isException            | 異常        | Boolean | 案件  | —           |
| requiredFieldStatus    | 必須フィールド状態 | Enum    | 案件  | 模块关→skipped |
| requiredDocumentStatus | 必要書類状態    | Enum    | 案件  | 同上          |
| missingDocuments       | 不足書類一覧    | Array   | 案件  | —           |
| missingFields          | 不足項目一覧    | Array   | 案件  | —           |
| aiVerifyResultJson     | 検証結果JSON  | Object  | 案件  | 关闭模块不写键     |
| files[].failedRules    | 失敗ルール     | Array   | 文件  | —           |




#### 条件判断 / 人工确认 / 通知 / 自定义函数


| 节点    | 主要输出变量（界面文案）                                      |
| ----- | ------------------------------------------------- |
| 条件判断  | 分岐名、分岐結果、命中ファイル件数                                 |
| 人工确认  | 確認状態、確認アクション、補件必要、異常、待確認ファイル/フィールド件数、files[].修正摘要 |
| 通知    | 送信状態、通知タイプ、送信日時、送信失敗理由、重複抑制                       |
| 自定义函数 | 用户定义出参 + ステータス、エラーメッセージ；ユーザーへ出力 OFF 时不暴露用户出参      |




#### 工作流测试

输入：仅内置标准集约済み用例，打开弹窗即加载，只读；不支持上传、选案或弹窗内编辑。

执行：用户点一次「テスト実行」→ 用例校验 → 沿可达路径模拟（人工确认标记跳过）→ 全画布扫描结论。成功=无失败节点且到达结束；否则失败。结论回写业务场景测试状态。

校验与展示（按层归类；失败以右侧时间线为主，孤立/不可达画布高亮）：


| 层级   | 代表阻断项                                  | 用户提示（日语，节选）           |
| ---- | -------------------------------------- | --------------------- |
| 用例   | 案件名空、文件清单空                             | 案件名を入力してください 等        |
| 结构   | 无结束、孤立、不可达、分支未接、回流不闭环                  | 終了未到達、接続のないノードがあります 等 |
| 节点配置 | 前处理/OCR/检证全关、条件空、人工确认上游非法、通知无收件人、函数不完整 | 前処理オプションを1件以上… 等      |
| 变量引用 | 不可达、不存在、类型不匹配                          | 変数が見つかりません 等          |
| 依赖   | 账票/模板/映射/检证/导出字段失效                     | 发布阻断或时间线节点失败          |
| 发布门禁 | 与上同规则集 + 测试结论非成功                       | ワークフローテストを完了してください    |


测试结论映射：任一阻断或模拟失败→测试失败→不可发布；仅警告且到达结束→可发布。

#### 异常与边界（画布交互）


| 场景        | 用户提示 / 处理                 |
| --------- | ------------------------- |
| 删除开始节点    | 「開始ノードは削除できません。」          |
| 连接成功 / 回流 | 「接続しました」/「回流接続しました」       |
| 拖拽连线提示    | 「接続先ノードの入力ポートへドラッグしてください」 |
| 节点删除成功    | 「ノードを削除しました」              |
| 非相邻节点交换   | 「メインフロー上の隣接ノードのみ入れ替えできます」 |
| 重置确认      | 「このステップの変更を破棄してリセットしますか？」 |
| 测试不写正式数据  | 禁止污染案件/待办/站内信列表           |


